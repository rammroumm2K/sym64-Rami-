{# templates/main/index.html.twig #}
{% include 'template.front.html.twig' %}

{% block carousel %}
<section class="dark pt-4">

		<h1 class="h1 text-center text-light m-6" id="pageHeaderTitle">Nos dernières Articles</h1>
     
{% for item in articles %}
	<div class="container py-4">
		<article class="postcard dark blue">
			<a class="postcard__img_link" href="#">
				<img class="postcard__img" src="{{asset('img/icons/plus.png')}}" alt="Profile pic blog" />
			</a>
			<div class="postcard__text">
				<h1 class="postcard__title blue"><a href="{{ path ('article_detail', {'id': item.id })}}"> Titre: {{ item.title}}</a></h1>
				<div class="postcard__subtitle small">
					<time datetime="2020-05-25 12:00:00">
						<i class="fas fa-calendar-alt mr-2"></i> Slug: {{ item.titleSlug }}
					</time>
				</div>
				<div class="postcard__bar"></div>
				<div class="postcard__preview-txt">{{ item.text}}</div>
				<ul class="postcard__tagbox">
					<li class="tag__item"><i class="fas fa-tag mr-2"></i>Écrit par {{item.User.fullname}}</li>
					<li class="tag__item"><i class="fas fa-clock mr-2"></i>Publié le: {{ item.articleDatePosted|date("d/m/Y \à H:i") }}</li>
					
				</ul>
			</div>
		</article>
	</div>
    {% else %}
    <div id="plus">
                    <div class="container-sm p-5">
                        <div class="container col-xxl-8 px-4 py-5">
                            <div class="row flex-lg-row-reverse align-items-center g-5 py-5">
                                <div class="col-10 col-sm-8 col-lg-6">
                                    <img src="{{ asset('img/fondo plus.webp') }}" class="d-block mx-lg-auto img-fluid rounded shadow"
                                        alt="Bootstrap Themes" width="700" height="500" loading="lazy">
                                </div>
                                <div class="col-lg-6">
                                    <h1 class="display-5 fw-bold text-body-emphasis lh-1 mb-3">Bienvenue sur Article Gamers</h1>
                                    <p class="lead">Pas d'article pour le moment!🙄
                
                                    </p>
                                </div>
                            </div>
                        </div>
                    </div>

    {% endfor %}
</section>

    {% endblock %}

 
        




